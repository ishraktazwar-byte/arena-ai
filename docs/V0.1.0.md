# V0.1.0 validation record

## Implemented
- Explicit authentication/configuration validation and Alice/Bob profiles.
- Persistent Mineflayer body with spawn/death/disconnect lifecycle events.
- Structured observations; nearby entities do not imply confirmed visibility.
- Exclusive action ownership, priority preemption, timeout and guarded writes.
- Operator-only bounded movement smoke test and stop/status/quit commands.
- Ignored environment secrets and authentication caches.

## Automated results
- Syntax checks: PASS.
- Six automated tests: PASS on sandbox Node 22.22.3 / npm 10.9.8.
- Windows CMD and user-specified Node 24.20.0: NOT YET TESTED.
- Live Minecraft 1.21.1 / Aternos: NOT YET TESTED; authentication mode needs confirmation.
- npm audit: six moderate findings in the Mineflayer authentication dependency
  chain, originating from uuid buffer-bound checks. No forced major dependency
  downgrade applied. Review upstream compatibility before attempting remediation.

## Live acceptance checklist (pending)
1. Owner confirms server authentication mode, bot permission, and server availability.
2. Alice connects and emits SPAWN; status reports position, health and inventory.
3. In a prepared safe flat area, step moves briefly then releases controls.
4. Stop preempts movement; death interrupts an action without resuming it on spawn.
5. Quit exits cleanly; disconnect releases control and does not reconnect-loop.
6. Repeat with Bob using an appropriate separate identity/account.

This release is an automated-tested foundation, NOT validated autonomous survival.
Do not advance to a live-validated claim without recording actual server results.
Next increment: V0.1.1 local survival after this live foundation gate.

## Live connection attempt — 2026-09-12
Owner confirmed cracked/offline mode enabled and server online. Alice attempted
connections from the sandbox to Rayan_609.aternos.me at configured port 25565 and
DNS SRV-advertised port 33908. Both ended with EPIPE before any SPAWN event.
Separate Minecraft status pings failed with EPIPE / ECONNRESET. DNS resolved to
185.107.192.169. No successful world connection, movement, or survival test took
place. Cause is not established (server/network/access restrictions need further
verification). Both agent processes exited; no bots remain connected.

Follow-up: tried the owner-provided DynIP bocaccio.aternos.host:33908.
Alice again disconnected with EPIPE before SPAWN. The DynIP resolved to the
same IP as the public hostname. A TCP socket could connect, but that alone does
not establish a successful Minecraft handshake. Direct-endpoint testing did
not resolve the issue; a local Windows client/bot test is needed to distinguish
sandbox access issues from server/protocol issues. No live acceptance gate passed.

## Network isolation follow-up
- Minecraft status requests to both Aternos and an unrelated public Minecraft
  endpoint failed with EPIPE/ECONNRESET.
- A minimal protocol-767 status handshake, independent of Mineflayer, received
  no response bytes; sockets closed immediately after writing.
- Negative control: reserved documentation address 192.0.2.1:25565 reported
  TCP connected in 1ms, then closed in 3ms after writing. This is strong evidence
  that socket connect events in this sandbox are mediated/synthetic, not proof
  of reaching the destination server.
- Leading diagnosis: sandbox outbound raw-TCP mediation/restriction. Exact
  infrastructure policy is not visible here. No application-level fix verified.
  Feature development paused pending an environment with working Minecraft TCP
  connectivity or platform-side resolution. Do not weaken server authentication
  or alter combat/runtime logic to address this transport failure.
